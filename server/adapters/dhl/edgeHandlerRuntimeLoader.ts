/** Public DHL facade; the selected binding owns private tester-program behavior. */
export { sendStatusEmailInProcess } from "#tester-program-email-binding";
export type { StatusEmailResult } from "#tester-program-email-binding";
