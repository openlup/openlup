/**
 * Tpay checkout build-time gates (scaffolding, simulator UI, one-click UI).
 * Re-exported from the central flag registry — see `src/lib/flags.ts`.
 */
export {
  tpayCheckoutScaffoldingEnabled,
  tpaySimulatorUiEnabled,
  tpayOneClickUiEnabled,
} from "@/lib/flags";
