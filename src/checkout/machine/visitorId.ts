// Compatibility path for configurator modules. Public pricing, live quote and
// checkout deliberately share one app-level assignment id.
export { getOrCreateVisitorId } from "@/lib/commerceVisitorId";
