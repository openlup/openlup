/**
 * Delivery-method selection preview gates.
 * Re-exported from the central flag registry — see `src/lib/flags.ts`.
 */
export { dhlOnlyDeliveryEnabled, inpostGeowidgetEnabled } from "@/lib/flags";
