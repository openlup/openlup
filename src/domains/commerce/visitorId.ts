// Compatibility surface for earlier commerce-domain imports. The persistent
// app-specific key remains outside the provider-neutral domain boundary.
export { getOrCreateVisitorId } from "@/lib/commerceVisitorId";
