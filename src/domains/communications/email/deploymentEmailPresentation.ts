// Stable deployment/runtime trace target. The immutable deployment projection
// rewrites only this file to select the private overlay; the source tree and
// published platform keep the neutral example.
export { emailPresentation } from "./exampleEmailPresentation.js";
